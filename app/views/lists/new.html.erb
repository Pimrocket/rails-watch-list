<div class="container">
  <%= simple_form_for @list do |f| %>
    <%= f.input :name %>
    <%# <%= f.submit "Create" class="btn btn-success"%>
    <%= f.submit %>
  <% end %>
  <p class="m-3"><%= link_to "Go back", lists_path %></p>
</div>
